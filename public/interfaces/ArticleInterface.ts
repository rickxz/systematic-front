interface ArticleInterface {
    title: string, 
    authors: string, 
    venue: string,
    year: string,
    selection: string,
    extraction: string,
    readingPriority: string;
}

export default ArticleInterface;